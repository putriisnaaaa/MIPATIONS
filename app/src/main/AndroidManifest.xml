<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.muliyani.putri.isna.mipations">

    <uses-permission android:name="android.permission.SET_WALLPAPER" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.Mipations">
        <activity android:name=".MainActivity" />
        <activity android:name=".akreditasi.AkreditasiInformatikaActivity" />
        <activity android:name=".AkreditasiActivity" />
        <activity android:name=".OrmawaActivity" />
        <activity android:name=".DosenActivity" />
        <activity android:name=".JurusanActivity" />
        <activity android:name=".VisiMisiActivity" />
        <activity android:name=".SejarahActivity" />
        <activity android:name=".SplashScreen">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>